<%= simple_form_for(@food) do |form| %>
  <%= form.input :name %>
  <%= form.input :serving %>
  <%= form.input :calories %>
  <!-- what do these args do? -->

  <%= form.submit %>
<% end %>

<%= form_tag(user_path(current_user), method: "get") do %>
  <%= text_field_tag(:criteria, nil) %>
  <%= submit_tag "Search", class:"search-button" %>
<% end %>
